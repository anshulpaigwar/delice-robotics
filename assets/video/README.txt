HERO BACKGROUND VIDEO

Put a short, muted, looping clip here named:
  hero.mp4   (H.264, 1080p, a few seconds, ~3-8 MB)
  hero.webm  (optional, smaller/sharper — add as well if you can)
Also add assets/images/hero-poster.jpg as the still shown before it loads.

Keep it short and compressed — GitHub Pages has a ~1 GB repo soft limit and
bandwidth caps. For anything longer, host on YouTube and link it in the Videos
section instead.

GIF ALTERNATIVE: if a video is awkward, in index.html replace the <video>...</video>
block with:  <img class="hero-video" src="assets/video/hero.gif" alt="">
